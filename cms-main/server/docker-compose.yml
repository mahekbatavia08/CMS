version: '3.8'

services:
  server:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Mount the local uploads folder to persist files on local disk
      - ./uploads:/app/uploads
      # Bind the .env file
      - ./.env:/app/.env
    environment:
      - NODE_ENV=development
